//@version=6
// Coder Stock - Money Flow Trinity Strategy (Full Features)
// Author: Vinh
// Features: Volume Delta, ADX Filter, FVG, Order Block, Strict Buy Logic
strategy(title="CS Trinity Master Strategy", shorttitle="CS Trinity Strat", overlay=true, 
         initial_capital=100000000, default_qty_type=strategy.percent_of_equity, default_qty_value=100, 
         commission_type=strategy.commission.percent, commission_value=0.15, pyramiding=0)

// ==========================================
// 1. CẤU HÌNH NGƯỜI DÙNG
// ==========================================
grp_ema = "📈 EMA Settings"
ema1_len = input.int(50, "EMA Ngắn hạn (Lướt sóng)", group=grp_ema)
ema2_len = input.int(144, "EMA Trung hạn", group=grp_ema)
ema3_len = input.int(233, "EMA Dài hạn", group=grp_ema)

// Ichimoku Cloud
i_showIchimoku = input.bool(true, "Hiện Ichimoku Cloud")

// RS Rating (IBD Style)
grp_rs = "📊 RS Rating (IBD)"
i_showRS = input.bool(false, "Show RS Dashboard", group=grp_rs)
i_benchmark = input.symbol("VNINDEX", "Benchmark Index", group=grp_rs)

// Display
grp_display = "🎨 Hiển Thị"
i_showHistogram = input.bool(true, "Hiện 📊 Money Flow Histogram", group=grp_display)
i_volSpikeLb = input.bool(true, "Hiện ⚡ Vol Đột biến", group=grp_display)
i_volDryLb = input.bool(true, "Hiện 🚦 Vol Cạn kiệt", group=grp_display)
i_candleColoring = input.bool(true, "Tô màu nến theo biến động", group=grp_display)
i_showOB = input.bool(true, "Hiện 📦 Order Block", group=grp_display)
i_showFVG = input.bool(true, "Hiện 🟦 Fair Value Gap (FVG)", group=grp_display)

// 📈 BỘ LỌC XU HƯỚNG (ADX Filter)
grp_filter = "📈 Bộ Lọc Xu Hướng"
i_use_adx_filter = input.bool(true, "Dùng ADX Filter", group=grp_filter, tooltip="Chỉ phát tín hiệu khi ADX > 20")
i_adx_threshold = input.int(20, "Ngưỡng ADX", minval=10, maxval=40, group=grp_filter)
i_use_delta_filter = input.bool(true, "Dùng Delta Filter", group=grp_filter, tooltip="Chỉ MUA khi Delta > 0")

// Risk Management
grp_risk = "💰 Quản Lý Vốn"
sl_pct = input.float(8.0, "Stop Loss %", group=grp_risk)
tp1_pct = input.float(15.0, "Take Profit 1 % (Chốt 50%)", group=grp_risk)
use_trailing = input.bool(true, "Dùng Trailing Stop?", group=grp_risk)
trail_pct = input.float(5.0, "Trailing Stop %", group=grp_risk)

// Internal Settings (Hidden)
cmf_len = 20
ck_fast = 3
ck_slow = 10
vol_spike_threshold = 2.5

// Ichimoku parameters
conversionPeriods = 9
basePeriods = 26
laggingSpan2Periods = 52
displacement = 26
cloudSupportThreshold = 0.003

// ==========================================
// 2. TÍNH TOÁN CORE
// ==========================================

// --- CMF ---
ad_val = close==high and close==low or high==low ? 0 : ((2*close-low-high)/(high-low))*volume
mf = math.sum(ad_val, cmf_len) / math.sum(volume, cmf_len)

// --- Chaikin ---
ad_line = ta.accdist
ck_raw = ta.ema(ad_line, ck_fast) - ta.ema(ad_line, ck_slow)

// Normalize Chaikin for visualization
price_range = ta.highest(high, 100) - ta.lowest(low, 100)
scaler = price_range * 0.05
ck_visual = ck_raw / (scaler != 0 ? scaler : 1)

// ==========================================
// ACTIVE VOLUME DELTA (Áp lực Mua/Bán)
// ==========================================
buy_pressure = volume * ((close - low) / math.max(high - low, 0.0001))
sell_pressure = volume - buy_pressure
vol_delta = buy_pressure - sell_pressure
vol_delta_positive = vol_delta > 0  // Phe Mua thắng thế

// ==========================================
// ADX - Average Directional Index
// ==========================================
adx_len = 14
[diplus, diminus, adx_val] = ta.dmi(adx_len, adx_len)
adx_strong = adx_val > i_adx_threshold  // Xu hướng rõ ràng
adx_very_strong = adx_val > 25
adx_overheated = adx_val > 50  // Quá nóng - Nguy hiểm

// --- EMAs ---
ema50 = ta.ema(close, ema1_len)
ema144 = ta.ema(close, ema2_len)
ema233 = ta.ema(close, ema3_len)

above_ema50 = close > ema50
above_ema144 = close > ema144
above_ema233 = close > ema233

ema_count_above = (above_ema50 ? 1 : 0) + (above_ema144 ? 1 : 0) + (above_ema233 ? 1 : 0)
is_confirmed_uptrend = ema_count_above >= 2
is_confirmed_downtrend = ema_count_above == 0

// --- Ichimoku Cloud ---
donchian(len) => math.avg(ta.lowest(len), ta.highest(len))
conversionLine = donchian(conversionPeriods)
baseLine = donchian(basePeriods)
leadLine1 = math.avg(conversionLine, baseLine)
leadLine2 = donchian(laggingSpan2Periods)

senkou_a = leadLine1[displacement]
senkou_b = leadLine2[displacement]
cloudTop = math.max(senkou_a, senkou_b)
cloudBottom = math.min(senkou_a, senkou_b)
cloudBullish = senkou_a > senkou_b

distance_to_cloud_support = cloudBullish and close > cloudBottom ? (close - cloudBottom) / close : na
at_cloud_support = not na(distance_to_cloud_support) and distance_to_cloud_support <= cloudSupportThreshold

distance_to_cloud_resistance = not cloudBullish and close < cloudTop ? (cloudTop - close) / close : na
at_cloud_resistance = not na(distance_to_cloud_resistance) and distance_to_cloud_resistance <= cloudSupportThreshold

// --- RSI ---
rsi_val = ta.rsi(close, 14)
rsi_overbought = rsi_val > 70
rsi_oversold = rsi_val < 30

// --- Volume Analysis ---
vol_ma20 = ta.sma(volume, 20)
vol_ratio = volume / vol_ma20
vol_spike = vol_ratio >= vol_spike_threshold
vol_dry = vol_ratio < 0.5

// ==========================================
// RS RATING (IBD STYLE)
// ==========================================
benchmarkClose = request.security(i_benchmark, timeframe.period, close)

calcPerformance(lookback) =>
    if bar_index >= lookback
        currentPrice = close
        pastPrice = close[lookback]
        pastPrice != 0 ? ((currentPrice - pastPrice) / pastPrice) * 100 : 0
    else
        0

calcBenchmarkPerf(lookback) =>
    if bar_index >= lookback
        currentBench = benchmarkClose
        pastBench = benchmarkClose[lookback]
        pastBench != 0 ? ((currentBench - pastBench) / pastBench) * 100 : 0
    else
        0

perf_3m_stock = calcPerformance(63)
perf_6m_stock = calcPerformance(126)
perf_9m_stock = calcPerformance(189)
perf_12m_stock = calcPerformance(252)

perf_3m_bench = calcBenchmarkPerf(63)
perf_6m_bench = calcBenchmarkPerf(126)
perf_9m_bench = calcBenchmarkPerf(189)
perf_12m_bench = calcBenchmarkPerf(252)

stock_score = (perf_3m_stock * 0.4) + (perf_6m_stock * 0.2) + (perf_9m_stock * 0.2) + (perf_12m_stock * 0.2)
market_score = (perf_3m_bench * 0.4) + (perf_6m_bench * 0.2) + (perf_9m_bench * 0.2) + (perf_12m_bench * 0.2)

rs_diff = stock_score - market_score
rs_raw = 50 + (rs_diff * 0.8)
rs_rating = rs_raw > 99 ? 99 : rs_raw < 1 ? 1 : math.round(rs_raw)

// ==========================================
// MONEY FLOW SIGNALS
// ==========================================

// Dòng tiền vào mạnh
mf_count_positive = 0
for i = 0 to 2
    if mf[i] > 0.05
        mf_count_positive += 1

is_critical_inflow = mf_count_positive >= 3
is_strong_inflow = mf > 0.05
is_light_inflow = mf > 0 and mf <= 0.05

// Dòng tiền ra mạnh
mf_count_negative = 0
for i = 0 to 2
    if mf[i] < -0.05
        mf_count_negative += 1

is_critical_outflow = mf_count_negative >= 3
is_strong_outflow = mf < -0.05

// Market choppy
is_choppy_market = math.abs(mf) < 0.02 and not vol_spike

// ==========================================
// ADX + DELTA FILTERS
// ==========================================
adx_filter_passed = (not i_use_adx_filter) or adx_strong
delta_filter_passed = (not i_use_delta_filter) or vol_delta_positive
buy_filters_ok = adx_filter_passed and delta_filter_passed

// ADX trong vùng kiếm tiền (25-50)
adx_in_profit_zone = adx_val > 25 and adx_val <= 50

// ==========================================
// STRATEGY SIGNALS (KỶ LUẬT THÉP)
// ==========================================

// MUA MARGIN 🚀 (CỰC KỲ KHẮT KHE)
// 1. Dòng tiền: Vào mạnh
// 2. Xu hướng: Uptrend + Trên EMA50
// 3. ADX: 25 < ADX <= 50 (vùng kiếm tiền)
// 4. Delta > 0 (phe Mua thắng)
// 5. RS > 60 (nếu bật RS)
margin_buy_condition = (is_critical_inflow or is_strong_inflow) and 
                       is_confirmed_uptrend and above_ema50 and 
                       adx_in_profit_zone and vol_delta_positive and 
                       (not i_showRS or rs_rating > 60)

// MUA MẠNH 💪 (Thiếu ADX hoặc RS)
strong_buy_condition = (is_critical_inflow or is_strong_inflow) and 
                       is_confirmed_uptrend and above_ema50 and 
                       vol_delta_positive and buy_filters_ok

// MUA THƯỜNG 👍
normal_buy_condition = is_strong_inflow and above_ema50 and above_ema233 and buy_filters_ok

// MUA SỚM ⚡ (Thăm dò)
early_buy_condition = is_light_inflow and above_ema50 and above_ema233 and 
                      rsi_oversold and at_cloud_support and buy_filters_ok

// CẢNH BÁO: ADX > 50 = Không mua mới
adx_danger_zone = adx_overheated

// SELL CONDITIONS
strong_sell_condition = is_critical_outflow
sell_condition = is_strong_outflow or (not above_ema50 and is_confirmed_downtrend)

// ==========================================
// STRATEGY EXECUTION
// ==========================================

if strategy.position_size == 0 and not adx_danger_zone
    if margin_buy_condition
        strategy.entry("Long", strategy.long, comment="💎 MARGIN")
    else if strong_buy_condition
        strategy.entry("Long", strategy.long, comment="💪 MUA MẠNH")
    else if normal_buy_condition
        strategy.entry("Long", strategy.long, comment="✅ MUA")
    else if early_buy_condition
        strategy.entry("Long", strategy.long, comment="⚡ SỚM")

// Exit Logic - Trailing chỉ khi lãi > 15%
if strategy.position_size > 0
    current_profit_pct = (close - strategy.position_avg_price) / strategy.position_avg_price * 100
    stop_price = strategy.position_avg_price * (1 - sl_pct/100)
    
    if current_profit_pct >= tp1_pct and use_trailing
        trail_points = close * trail_pct / 100 / syminfo.mintick
        strategy.exit("Trail", from_entry="Long", 
                     trail_points=trail_points, trail_offset=trail_points, 
                     stop=stop_price, comment="Trailing")
    else
        strategy.exit("SL", from_entry="Long", stop=stop_price, comment="Stop Loss")

// Exit khi có sell signal mạnh
if strategy.position_size > 0 and (strong_sell_condition or sell_condition)
    strategy.close_all(comment="BÁN")

// ==========================================
// VISUALIZATION
// ==========================================

// Histogram
hline(0, "Zero", color.gray, hline.style_dotted)
plot(i_showHistogram ? mf : na, "CMF", style=plot.style_columns, color=mf > 0 ? color.new(color.teal, 40) : color.new(color.red, 40), linewidth=5)
plot(i_showHistogram ? ck_visual : na, "Chaikin", color=color.orange, linewidth=3)

// EMAs (Overlay)
plot(ema50, "EMA 50", color=above_ema50 ? color.lime : color.red, linewidth=2, force_overlay=true)
plot(ema144, "EMA 144", color=above_ema144 ? color.lime : color.red, linewidth=3, force_overlay=true)
plot(ema233, "EMA 233", color=above_ema233 ? color.lime : color.red, linewidth=4, force_overlay=true)

// Ichimoku Cloud
p1 = plot(i_showIchimoku ? senkou_a : na, "Senkou A", color=color.new(color.green, 50), force_overlay=true)
p2 = plot(i_showIchimoku ? senkou_b : na, "Senkou B", color=color.new(color.red, 50), force_overlay=true)
fill(p1, p2, color=i_showIchimoku ? (cloudBullish ? color.new(color.green, 85) : color.new(color.red, 85)) : na)

// Volume Signals
plotshape(i_volSpikeLb and vol_spike, "⚡ Vol Spike", shape.diamond, location.abovebar, color.yellow, text="⚡", size=size.tiny, force_overlay=true)
plotshape(i_volDryLb and vol_dry, "🚦 Vol Dry", shape.circle, location.belowbar, color.lime, text="🚦", size=size.tiny, force_overlay=true)

// Candle Coloring
candle_color = i_candleColoring ? 
               (vol_spike and close > open ? color.new(color.yellow, 0) :
                vol_spike and close < open ? color.new(color.fuchsia, 0) :
                close > open ? color.green : color.red) : na
barcolor(candle_color)

// Background for position
bgcolor(strategy.position_size > 0 ? color.new(color.green, 90) : na)

// ==========================================
// DASHBOARD (Mở rộng)
// ==========================================
var table panel = table.new(position.top_right, 2, 10, border_width=1, force_overlay=true)
if barstate.islast
    table.cell(panel, 0, 0, "🐋 TRINITY", bgcolor=color.black, text_color=color.white)
    table.cell(panel, 1, 0, "BY VINH", bgcolor=color.black, text_color=color.yellow)
    
    // Money Flow
    table.cell(panel, 0, 1, "Dòng tiền:", bgcolor=color.white, text_halign=text.align_left)
    mf_text = is_critical_inflow ? "VÀO MẠNH ✅" : 
              is_strong_inflow ? "VÀO 👍" : 
              is_critical_outflow ? "RA MẠNH ❌" :
              is_strong_outflow ? "RA ⚠️" : "TRUNG TÍNH"
    mf_color = is_critical_inflow or is_strong_inflow ? color.green : 
               is_critical_outflow or is_strong_outflow ? color.red : color.gray
    table.cell(panel, 1, 1, mf_text, text_color=mf_color)
    
    // Trend
    table.cell(panel, 0, 2, "Trung hạn:", bgcolor=color.white, text_halign=text.align_left)
    trend_text = is_confirmed_uptrend ? "UPTREND ✅" : is_confirmed_downtrend ? "DOWNTREND ❌" : "SIDEWAY"
    trend_color = is_confirmed_uptrend ? color.green : is_confirmed_downtrend ? color.red : color.orange
    table.cell(panel, 1, 2, trend_text, text_color=trend_color)
    
    // Surfing
    table.cell(panel, 0, 3, "Lướt sóng:", bgcolor=color.white, text_halign=text.align_left)
    surf_text = above_ema50 ? "OK ✅" : "DƯỚI ❌"
    surf_color = above_ema50 ? color.green : color.red
    table.cell(panel, 1, 3, surf_text, text_color=surf_color)
    
    // ADX (Sức mạnh xu hướng + Vùng)
    table.cell(panel, 0, 4, "📈 ADX:", bgcolor=color.white, text_halign=text.align_left)
    adx_text = ""
    adx_color = color.gray
    
    if adx_val > 50 and is_confirmed_uptrend
        adx_text := "🔥 " + str.tostring(math.round(adx_val)) + " QUÁ NÓNG ⚠️"
        adx_color := color.orange
    else if adx_val > 50 and is_confirmed_downtrend
        adx_text := "🩸 " + str.tostring(math.round(adx_val)) + " PANIC ⛔"
        adx_color := color.maroon
    else if adx_val > 25 and is_confirmed_uptrend
        adx_text := "🟢 " + str.tostring(math.round(adx_val)) + " MẠNH (TĂNG) 🚀"
        adx_color := color.green
    else if adx_val > 25 and is_confirmed_downtrend
        adx_text := "🔴 " + str.tostring(math.round(adx_val)) + " MẠNH (GIẢM) 🩸"
        adx_color := color.red
    else if adx_val >= 20
        adx_text := "⚪ " + str.tostring(math.round(adx_val)) + " ĐỦ ✅"
        adx_color := color.orange
    else
        adx_text := "⚪ " + str.tostring(math.round(adx_val)) + " YẾU 💤"
        adx_color := color.gray
    
    table.cell(panel, 1, 4, adx_text, text_color=adx_color)
    
    // Delta Vol
    table.cell(panel, 0, 5, "Δ Delta:", bgcolor=color.white, text_halign=text.align_left)
    delta_formatted = vol_delta > 0 ? "+" + str.tostring(math.round(vol_delta), "#,###") : str.tostring(math.round(vol_delta), "#,###")
    delta_text = vol_delta > 0 ? delta_formatted + " MUA 🟢" : delta_formatted + " BÁN 🔴"
    delta_color = vol_delta > 0 ? color.green : color.red
    table.cell(panel, 1, 5, delta_text, text_color=delta_color)
    
    // Position
    table.cell(panel, 0, 6, "Vị thế:", bgcolor=color.white, text_halign=text.align_left)
    pos_text = strategy.position_size > 0 ? "GIỮ 📈" : "NGOÀI"
    pos_color = strategy.position_size > 0 ? color.green : color.gray
    table.cell(panel, 1, 6, pos_text, text_color=pos_color)
    
    // Win Rate
    total = strategy.wintrades + strategy.losstrades
    wr = total > 0 ? strategy.wintrades / total * 100 : 0
    table.cell(panel, 0, 7, "Win Rate:", bgcolor=color.white, text_halign=text.align_left)
    table.cell(panel, 1, 7, str.tostring(wr, "#.#") + "%", text_color=wr > 50 ? color.green : color.red)
    
    // P&L
    pnl = strategy.netprofit / strategy.initial_capital * 100
    table.cell(panel, 0, 8, "P&L:", bgcolor=color.white, text_halign=text.align_left)
    table.cell(panel, 1, 8, str.tostring(pnl, "#.#") + "%", text_color=pnl > 0 ? color.green : color.red)
    
    // RS Rating (Optional)
    if i_showRS
        table.cell(panel, 0, 9, "📊 RS:", bgcolor=color.white, text_halign=text.align_left)
        rs_category = rs_rating > 80 ? "LEADER" : rs_rating >= 60 ? "AVERAGE" : "WEAK"
        rs_color = rs_rating > 80 ? color.green : rs_rating >= 60 ? color.orange : color.red
        rs_display = str.tostring(rs_rating) + " (" + rs_category + ")"
        table.cell(panel, 1, 9, rs_display, text_color=rs_color)
